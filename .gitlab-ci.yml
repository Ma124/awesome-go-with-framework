image: golang:latest

pages:
  stage: deploy
  script:
  - go get gitlab.com/Ma_124/awesome-framework/cmd/awesome
  - $GOPATH/bin/awesome compile --gh-max-reqs 1
  - cp -r tmpl public
  - cp tmpl/index.html public/index.html
  artifacts:
    paths:
    - public
  only:
  - master

